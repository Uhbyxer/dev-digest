# Insights — client

Non-deterministic observations from actual work on this module: "tried X,
got Y." Format: date, what was done, what was learned.

Once an observation becomes a stable rule, don't leave it here: move it to
`CLAUDE.md` (if it's a map fact) or a skill/slash command (if it's a
process).
